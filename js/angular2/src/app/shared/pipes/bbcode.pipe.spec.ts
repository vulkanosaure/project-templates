import { BbcodePipe } from './bbcode.pipe';

describe('BbcodePipe', () => {
  it('create an instance', () => {
		
		
    const pipe = new BbcodePipe([
			
		]);
    expect(pipe).toBeTruthy();
  });
});
