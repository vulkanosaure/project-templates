import { ScaledDirective } from './scaled.directive';
/* 
describe('ScaledDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaledDirective();
    expect(directive).toBeTruthy();
  });
});
 */