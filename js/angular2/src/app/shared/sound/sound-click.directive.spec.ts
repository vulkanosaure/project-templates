import { SoundClickDirective } from './sound-click.directive';

describe('SoundClickDirective', () => {
  it('should create an instance', () => {
    const directive = new SoundClickDirective();
    expect(directive).toBeTruthy();
  });
});
